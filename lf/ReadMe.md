# Small Scripts to Fix common Issues

## Fix Brave Launcher

- Deletes `Singleton` Files under `~/.config/BraveSoftware/Brave-Browser`
  - `rm SingletonCookie`
  - `rm SingletonLock`
  - `rm SingletonSocket`
